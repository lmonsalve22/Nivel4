@model IEnumerable<Nivel4.Models.LEVEL1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAME_LEVEL1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAME_LEVEL1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_LEVEL1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_LEVEL1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_LEVEL1 })
        </td>
    </tr>
}

</table>
