@model IEnumerable<Nivel4.Models.LEVEL4>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAME_LEVEL4)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VINCULOPOWERBI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TAG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LEVEL3.NAME_LEVEL3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAME_LEVEL4)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VINCULOPOWERBI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TAG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LEVEL3.NAME_LEVEL3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_LEVEL4 }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_LEVEL4 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_LEVEL4 })
        </td>
    </tr>
}

</table>
