@model IEnumerable<Nivel4.Models.LEVEL2>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAME_LEVEL2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LEVEL1.NAME_LEVEL1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAME_LEVEL2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LEVEL1.NAME_LEVEL1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_LEVEL2 }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_LEVEL2 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_LEVEL2 })
        </td>
    </tr>
}

</table>
